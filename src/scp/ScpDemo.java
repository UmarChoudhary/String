package scp;

public class ScpDemo {

//if i created the one string literal and then re-create the the same string with new keyword, in that case what will happen in the literal
	// will it create new one in SCP(for future reference) or just point it to the alredy create string in SCP
}
